{
  "name": "order-ice-cream-app",
  "version": "1.0.0",
  "description": "Aplicacion para la clase diseno y analisis de sistemas",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd server && npm install --only=dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npm run --prefix ./server/ start",
    "server-test":"npm run --prefix ./server test"
  },
  "author": "Ivan V",
  "license": "ISC",
  "dependencies": {
    "ice-cream-server": "file:server"
  }
}
